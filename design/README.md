# Design sources (reference only — not packaged into the VSIX)

The build is ported from these. Drop the originals in here:

- `YIELD-BUILD-HANDOFF.md` — the spec. Tokens (§1), states (§2), copy (§3),
  chat mechanics (§4), reply model (§5), footer (§6), integration (§7),
  gotchas (§8), phases (§9), open decisions (§10).
- `yield-panel-mock-v77.html` — visual source of truth (exact CSS/HTML/motion).
- `yield-state-model.html` — agent/user timeline behaviour + collision rules.
- `yield-conversational-flow.html` — scripted chat blueprint.

The logo is NOT here: it ships, so it lives at `media/yield-logo.svg`.
